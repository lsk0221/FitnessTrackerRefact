# Environment Variables for FitnessTracker App
# Copy this file to .env and update with your values

# Cloudflare Workers API Endpoint
# The backend URL for authentication and data synchronization
EXPO_PUBLIC_API_URL=https://fitness-tracker-api.fitness-tracker.workers.dev

# Google OAuth Client ID
# Get this from Google Cloud Console > APIs & Services > Credentials
# Use the Web Client ID (not iOS/Android Client ID)
EXPO_PUBLIC_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE

# Google OAuth iOS Client ID
# Get this from Google Cloud Console > APIs & Services > Credentials
# Use the iOS Client ID (not Web Client ID)
# The REVERSED_CLIENT_ID is the reversed iOS Client ID (e.g., com.googleusercontent.apps.CLIENT_ID)
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=YOUR_GOOGLE_IOS_CLIENT_ID_HERE
